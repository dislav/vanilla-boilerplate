import RootComponent from '@common/rootComponent';
import { IRootComponent } from '@common/types';

class SwiperSlide extends RootComponent {
    constructor(props: IRootComponent) {
        super(props);
    }
}

export default SwiperSlide;
